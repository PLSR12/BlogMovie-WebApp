import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 48px;
  padding: 0 20px;
  width: 100%;
  position: relative;
  background: var(--color-background-paper);
  border-bottom: var(--border-width-small) solid var(--color-neutral-200);
`;
